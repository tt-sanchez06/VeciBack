PORT=3000
JWT_SECRET=change_me
# Comma-separated list of allowed origins (Render API + Vercel frontend)
CORS_ORIGINS=https://tu-frontend.vercel.app

# MySQL (Render managed database)
MYSQLHOST=your-mysql-host
MYSQLPORT=3306
MYSQLUSER=your-mysql-user
MYSQLPASSWORD=your-mysql-password
MYSQLDATABASE=your-db-name

# For HTTPS (optional, used only in self-hosted environments)
# SSL_KEY=./certs/key.pem
# SSL_CERT=./certs/cert.pem
